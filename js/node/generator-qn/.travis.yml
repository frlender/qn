language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-qn' ]; then cd .. && eval "mv $currentfolder generator-qn" && cd generator-qn; fi
